import React from "react";
import CircularProgressContainer from "./circularProgressContainer";

const CircularProgress = (props) => <CircularProgressContainer {...props} />;

export default CircularProgress;
